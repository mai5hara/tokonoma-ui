import { ClassNames } from 'react-day-picker';
/** Popover panel (matches Select dropdown chrome). */
export declare const dateRangePickerContent: string;
/** Tailwind class map for react-day-picker (single-month range). */
export declare const calendarClassNames: Partial<ClassNames>;
